{
  "name": "gui",
  "description": "Toy UI library",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.5.1"
  },
  "scripts": {
    "dev": "npx lerna run --parallel dev --stream",
    "clean": "npx lerna run rm && npx lerna clean --yes && rimraf node_modules"
  }
}
